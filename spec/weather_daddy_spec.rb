require 'spec_helper'

describe WeatherDaddy do
  it 'has a version number' do
    expect(WeatherDaddy::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
